/**
 * This is the main entry point for the dev-contracts-spec module.
 */

console.log("Hello from dev-contracts-spec!");

export function greet(name: string = "world"): string {
  return `Hello, ${name}!`;
} 
